/* Bubblesort pseudocode

O(n^2) whenn n is large it is not practical 

Bublesort (A list)
	repent 
	swapped = false;
		for(i=1 to length(A)-1 do 
			if A[i-1] > A[i]
				swap(A[i-1], A[i])
				swapped true;

			end if
		end for
	until not swapped
end procedure 


 */

